# Models Layer

SQLAlchemy ORM models representing database tables.

**Responsibility:** Define the database schema, relationships, and constraints.

**Key principle:** Models are data mapping constructs only. Business logic and validation rules belong in the Service and Schema layers respectively.

**Contents:**
- SQLAlchemy ORM model classes
- Table definitions for teams, matches, predictions, results, scores, evaluations, leaderboard
